[project]
name = "funserver"
version = "1.0.33"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [ "funbuild>=1.5.13",]

[project.scripts]
funserver = "funserver.base:funserver"
